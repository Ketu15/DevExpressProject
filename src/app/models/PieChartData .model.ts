// Define PieChartData interface
export interface PieChartData {
    title: string;
    palette: string[];
    dataSource: any[]; // You might want to specify a more specific type for dataSource
    // Other properties if needed
  }
  